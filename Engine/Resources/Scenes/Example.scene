meta_start
version 0.0.1
name "Example Scene"
camera Main

meta_end

meta
{
    version 0.0.1
    name "Example Scene"
    camera Main
}

entity Main
{
    add Transform vec3(0, 0, -5) vec3(0) vec3(0)
    add Camera 0   0.1 100.0   90. vec2(0)
}
add Transform vec3(0, 0, -5) vec3(0) vec3(0)

add PlayerController 2.6
add CameraController 10. true

entity Cube
add Transform vec3(-1, 0, 0) vec3(1) vec3(0)
add Renderer mesh(Objects/cube.obj) shader(unlit)
#add Rigidbody 0.

entity Cube2
add Transform vec3(1, 0, 0) vec3(1) vec3(0)
add Renderer mesh(Objects/cube.obj) shader(unlit)
add Rigidbody 0.